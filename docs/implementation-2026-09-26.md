# 优化实施记录

日期：2026-09-26。对应 [2026-09-25 审查计划](./optimization-plan-2026-09-25.md)。旧计划保留当时的源码位置、探针和未修复状态，本文件记录后续实现，不改写历史结论。

## 已完成

| 项目 | 实现 | 回归覆盖 |
| --- | --- | --- |
| EBR-01 | 原文偏移子句、条件范围继承、全部候选遍历、跳过原因 | 三个原漏报、否定、跨逗号/分号条件、引用、代码、CRLF/emoji |
| EBR-02 | 每次调用的行索引、惰性分句缓存、日期评估缓存、引用和问题提前去重、数量关系索引 | 1/16/128 重复引用、不同引用范围/用途保留、跨调用不复用材料 |
| EBR-03 | 明确单位及完整来源标签筛选后再判断集合歧义 | 不同单位不干扰、同单位显式来源绑定、无来源继续跳过、API/AB 不误作 A |
| EBR-04 | 最多 64 位的规范十进制字符串比较 | 安全整数边界两侧、千分位、负数、小数、错格式、超长数量明确跳过 |
| EBR-05 | `ReviewInputError`、JSON Pointer 路径、版本化 stderr 错误 JSON、离线调用示例 | JSON/UTF-8/文件/引用错误区分、原文与本机路径不回显、TypeError 兼容 |

版本改为 `0.2.0`，规则集改为 `evidence-bound-review/v2`。原规则 ID、退出码和 `factVerification: not_performed` 保留。`sources` 改为唯一绑定列表，新增原始/唯一引用和评估材料计数；中英 README 已注明。唯一问题上限统一为 2,000（含日期问题），不提高任何原有输入上限。

## 本地结果

- 全量离线 Node 测试 132 项通过，TypeScript 消费契约检查通过。
- CLI 三问题合成样例和结构化错误接入样例符合预期。
- 92,489 单元材料的 1/16/128 引用中位耗时为 26.41 / 29.27 / 29.39 ms，局部相对门槛通过；详见 [验证记录](../VERIFICATION.md)。
- 本轮 $0，不访问网络、邮箱或模型，不安装依赖、不生成构建缓存、不提交或推送。

## 保持的边界与待办

- 仍是有限中文办公约束检查，不是通用语义、算术、真实性、来源可信度或报告质量认证。
- 混合条件、嵌套 Markdown、复杂否定和表格语义仍可能漏检；规则未命中不能作为自动放行证明。
- Node 22/Linux 发布矩阵未运行，当前仅核对 Windows/Node 24。
- 本次只改独立仓库；TAgent 主仓库、`G:\tagent\opensource` 内嵌副本和统一产品验收未在本轮处理。
